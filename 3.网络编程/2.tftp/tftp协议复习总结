一.tftp协议
    下载：操作码(1)占两个字节 文件名 0 模式(octet) 0
    上传：操作码(2)占两个字节 文件名 0 模式(octet) 0
    服务器发回来的数据包：操作码(3)占两个字节 块编号占两个字节 数据512个字节
    ACK确认包：操作码(4)占两个字节 块编号
    ERROR：操作码(5) 差错码占两个字节 差错信息 0
二.小端和大端
    大端：数据的高字节存放在内存的低地址中
    小端：数据的高字节存放在内存的高地址中
    规定：在多字节时用大端进行