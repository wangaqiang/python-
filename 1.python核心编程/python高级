一.is和==
    ==是判断值是否相等，而is是判断是不是一个东西。
二.深拷贝和浅拷贝
    深拷贝特点是递归拷贝，即有多少层拷贝多少层，copy模块里的deepcopy为深拷贝
    copy模块里的copy为浅拷贝，它对可变类型如列表等只进行第一层的拷贝，而对于不可变类型如元组等连第一层都不拷贝，相当于a=b之间的直接指向。
三.原码，反码，补码
    正数：最左边符号位为0，原码=反码=补码
    负数：最左边符号位为1，反码为符号为不变，其余位置取反，补码为反码+1.
    从补码变回原码：符号为不变，其他位取反，然后加1
四.进制之间的转换
    十进制转二进制，八进制，十六进制为bin(),oct(),hex()得到，0b，0o，0x开头的数值
    二进制，八进制，十六进制转换十进制int('要转换的值',要转换的进制)
    十进制转二进制除二取余，除到商为0停止，余数从最后一个写到第一个即为二进制。二进制转换十进制为相乘后再相加
    二进制转八进制，取三位数按照二进制转十进制的方法合成,然后不足三个数的时候补0,最后结果为八进制。八进制转二进制，按照十进制转二进制的方法对每位进行转换成三个数，不足三个时在左边补0。最后得到二进制。
    二进制转十六进制，按照二进制转换八进制的方法，取四合一，不足补0.十六进制转二进制，将一个位拆成4个，不足补0.
    十进制转八进制或者十六进制，除8或这16,取余，八进制或十六进制转十进制，相乘相加。
    八进制转十六进制可以间接转换。
五.位运算
    位左移相当于乘2，右移相当于除2,异或^不同为1,相同为0.按位与&有0则0,按位或|有1则1.取反是对补码进行。